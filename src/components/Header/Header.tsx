import React from "react";
import { PixemaLight } from "assets";
import { HeaderContainer } from "./styles";

export const Header = () => {
  return (
    <HeaderContainer>
      <PixemaLight />
    </HeaderContainer>
  );
};
